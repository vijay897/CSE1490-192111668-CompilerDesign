#include <stdio.h>

int main() {
    int a, b, c;
    printf("Enter three integers: ");
    scanf("%d %d %d", &a, &b, &c);
    
    int result = (a + b) * c - a / b;
    printf("Result: %d\n", result);
    
    return 0;
}
